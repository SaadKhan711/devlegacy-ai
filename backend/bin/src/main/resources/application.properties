# Set the server port to be dynamic for production servers
server.port=${PORT:8080}

# Set the context path for production routing
server.servlet.context-path=/

# Your secrets (these will be overridden by Railway's variables)
spring.data.mongodb.uri=${spring_data_mongodb_uri}
gemini.api.key=${gemini_api_key}